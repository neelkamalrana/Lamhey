{
  "name": "lamhey",
  "version": "1.0.0",
  "description": "A modern web application with React frontend and Node.js backend",
  "main": "server/server.js",
  "scripts": {
    "dev": "concurrently \"npm run server:dev\" \"npm run client:dev\"",
    "server:dev": "cd server && npm run dev",
    "client:dev": "cd client && npm start",
    "build": "cd client && npm run build",
    "start": "cd server && npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "setup": "npm run install:all",
    "clean": "rm -rf client/build && rm -rf node_modules && rm -rf server/node_modules && rm -rf client/node_modules"
  },
  "keywords": ["react", "nodejs", "tailwind", "express", "webapp", "cognito"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
